---
- hosts: cisco
  become: yes
  gather_facts: no

  tasks:
    - name: Configure SNMPv3 "ZABBIX" group on Cisco devices
      cisco.ios.ios_snmp_server:
        config:
          groups:
            - group: "ZABBIX"
              version: "v3"
              version_option: "priv"
        state: replaced
    
    - name: Configure SNMPv3 user on Cisco devices
      cisco.ios.ios_snmp_server:
        config:
          users:
            - username: "zabbixlab_user"
              group: "ZABBIX"
              version: "v3"            
              authentication:
                algorithm: "sha"
                password: "lab123auth"
              encryption:
                priv: "aes"
                password: "lab123enc"
        state: replaced

    - name: Save running to startup when changed
      cisco.ios.ios_config:
        save_when: modified
